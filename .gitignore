# SuperDesign (VS Code extension) drops these at the repo root when
# initialized — tool artifacts, not project code, so keep them untracked.
.cursor/
.windsurfrules
.superdesign/

# Local git worktrees for isolated feature work — not project code
.worktrees/
